import { {{properCase name}} } from '@layouts';

import { render, screen } from "@test/utils";

describe('<{{properCase name}} />', () => {
  it("matches the snapshot", () => {
    const view = render(<{{properCase name}} />);

    expect(view).toMatchSnapshot();
  });
});